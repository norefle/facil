# fl create puts card in backlog

## Description

Create command should also create marker file inside initial board (backlog by default).

## Acceptance criteria

* Unit tests
* Cli puts new card in backlog.
